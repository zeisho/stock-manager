<%= form_for(@stock) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
    <%= f.label :name, "キット名" %>
    <%= f.text_field :name %>
  </div>
  
  <%= f.submit "追加", class: "btn btn-primary" %>
<% end %>